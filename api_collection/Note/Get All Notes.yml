info:
  name: Get All Notes
  type: http
  seq: 1

http:
  method: GET
  url: http://127.0.0.1:8000/api/notes
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
